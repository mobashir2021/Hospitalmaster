@*@model IEnumerable<HospitalManagementSys.Models.subcasetype>*@
@model PagedList.IPagedList<HospitalManagementSys.Models.subcasetype>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>Sub Casetype List</h3>
<br />
@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<div class="form-group">
    <div class="col-md-offset-2 col-md-10">
        <input type="submit" value="Create New Subcasetype" class="btn btn-primary"
               onclick="location.href='@Url.Action("Create", "subcasetypes")'" />
    </div>
</div>
<br />
<table class="table table-striped">
    <tr>
        <th>
            Sub Casetype
        </th>
        <th>
            Amount
        </th>
        <th>
            Case Type
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Subcasetype1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CaseType.CaseType1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubcasetypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubcasetypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubcasetypeId })
        </td>
    </tr>
}

</table>

<div style="background-color:azure;display:inline-block;border-radius:2px;">
    Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount
    @if (Model.HasPreviousPage)
    {
        @Html.ActionLink("   <<", "Index", new { page = 1 })
        @Html.Raw(" ");
        @Html.ActionLink("< Prev", "Index", new { page = Model.PageNumber - 1 })
    }
    else
    {
        @:<<
        @Html.Raw(" ");
        @:< Prev
    }

    @if (Model.HasNextPage)
    {
        @Html.ActionLink("Next >", "Index", new { page = Model.PageNumber + 1 })
        @Html.Raw(" ");
        @Html.ActionLink(">>", "Index", new { page = Model.PageCount })
    }
    else
    {
        @:Next >
        @Html.Raw(" ")
        @:>>
    }
</div>
