@*@model IEnumerable<HospitalManagementSys.Models.SUPPLIER>*@
@model PagedList.IPagedList<HospitalManagementSys.Models.SUPPLIER>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Supplier
        </th>
        <th>
            Company
        </th>
        <th>
            Mobile No
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SUPPLIERNAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.COMPANYNAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MOBILENO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SUPPLIERID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SUPPLIERID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SUPPLIERID })
        </td>
    </tr>
}

</table>

<div style="background-color:azure;display:inline-block;border-radius:2px;">
    Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount
    @if (Model.HasPreviousPage)
    {
        @Html.ActionLink("   <<", "Index", new { page = 1 })
        @Html.Raw(" ");
        @Html.ActionLink("< Prev", "Index", new { page = Model.PageNumber - 1 })
    }
    else
    {
        @:<<
        @Html.Raw(" ");
        @:< Prev
    }

    @if (Model.HasNextPage)
    {
        @Html.ActionLink("Next >", "Index", new { page = Model.PageNumber + 1 })
        @Html.Raw(" ");
        @Html.ActionLink(">>", "Index", new { page = Model.PageCount })
    }
    else
    {
        @:Next >
        @Html.Raw(" ")
        @:>>
    }
</div>
